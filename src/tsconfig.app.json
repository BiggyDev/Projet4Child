{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "*": ["types/*"] },
    "baseUrl": ".",
    "paths": { "*": ["types/*"] },
    "outDir": "../out-tsc/app",
    "types": [
      "leaflet",
      "googlemaps"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
